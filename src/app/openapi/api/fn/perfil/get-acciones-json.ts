/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ListaAccionDtoListApiResponse } from '../../models/lista-accion-dto-list-api-response';

export interface GetAcciones$Json$Params {
}

export function getAcciones$Json(http: HttpClient, rootUrl: string, params?: GetAcciones$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ListaAccionDtoListApiResponse>> {
  const rb = new RequestBuilder(rootUrl, getAcciones$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ListaAccionDtoListApiResponse>;
    })
  );
}

getAcciones$Json.PATH = '/api/Perfil/GetAcciones';
