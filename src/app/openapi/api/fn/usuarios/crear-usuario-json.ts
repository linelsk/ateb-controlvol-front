/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CrearUsuarioDto } from '../../models/crear-usuario-dto';
import { CrearUsuarioDtoApiResponse } from '../../models/crear-usuario-dto-api-response';

export interface CrearUsuario$Json$Params {
      body?: CrearUsuarioDto
}

export function crearUsuario$Json(http: HttpClient, rootUrl: string, params?: CrearUsuario$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<CrearUsuarioDtoApiResponse>> {
  const rb = new RequestBuilder(rootUrl, crearUsuario$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CrearUsuarioDtoApiResponse>;
    })
  );
}

crearUsuario$Json.PATH = '/api/Usuarios/CrearUsuario';
